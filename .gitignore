# Build output
dist/
.astro/

# Dependencies
node_modules/

# Logs
*-debug.log*
logs/

# Variables d'environnement (jamais commitées — tokens Storyblok, etc.)
.env
.env.*.local
.env.production

# Vercel
.vercel

# macOS
.DS_Store

# IDE JetBrains
.idea/

# Config locale Claude Code (par poste, non partagée)
.claude/settings.local.json

# Dossier de travail photo (hors public/ pour ne pas alourdir le build) — JAMAIS commité.
# originals/ = bruts lourds ; optimized/ = dérivés WebP destinés à l'upload Storyblok.
# Les images du site final vivent dans les assets Storyblok, pas dans le repo.
photos-src/

# Certificats HTTPS de dev (mkcert) — locaux, non commités
certs/
