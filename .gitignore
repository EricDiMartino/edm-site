# Build output
dist/
.astro/

# Dependencies
node_modules/

# Logs
*-debug.log*
logs/

# Variables d'environnement (jamais commitées — tokens Storyblok, etc.)
.env
.env.*.local
.env.production

# Vercel
.vercel

# macOS
.DS_Store

# IDE JetBrains
.idea/

# Config locale Claude Code (par poste, non partagée)
.claude/settings.local.json

# Photos sources brutes (lourdes, prototype only) — JAMAIS commitées.
# Les images du site viennent des assets Storyblok ; ces fichiers servent à
# l'intégration/optimisation locale. Les dérivés optimisés seront gérés à part.
public/PHOTO/
