# Build output
dist/
.astro/

# Dependencies
node_modules/

# Logs
*-debug.log*
logs/

# Variables d'environnement (jamais commitées — tokens, clés API, etc.)
.env
.env.*.local
.env.production

# Vercel
.vercel

# macOS
.DS_Store

# IDE JetBrains
.idea/

# Config locale Claude Code (par poste, non partagée)
.claude/settings.local.json

# Dossier de travail photo (hors public/ pour ne pas alourdir le build) — JAMAIS commité.
# originals/ = bruts lourds ; optimized/ = dérivés WebP. Les images utilisées sont
# copiées dans src/assets/ (versionnées) ; les bruts restent hors du repo.
photos-src/
